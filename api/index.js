// Vercel entry point. Zero-config builds functions from this directory, so the
// Express app lives here rather than being rewritten to a file Vercel does not
// treat as a function. Locally `npm start` runs app.js directly instead.
export { default } from '../app.js';
